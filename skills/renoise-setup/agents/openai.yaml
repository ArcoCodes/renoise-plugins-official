interface:
  display_name: "Setup / Account"
  short_description: "Install or update the CLI, sign in, and check readiness"
  default_prompt: "Use $renoise-setup to set up, update, or diagnose my Renoise account."
policy:
  allow_implicit_invocation: false
